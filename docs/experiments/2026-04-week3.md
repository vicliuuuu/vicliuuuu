# 四月第三周实验记录

## 4.13 实现了图像流式输入与点云的叠加建立

虽然现在仍然处于初步探索阶段，但是整体的效果还是看的过去；已经验证了初步的可行性。

```text

 python run_image_inference.py --image-dir D:\3DV\STream3R\output\case0\images --output-dir D:\3DV\STream3R\output\case01 --mode window --save-glb --incremental --incremental-backend stream --frames-per-batch 3 --fusion icp_voxel --skip-finalize-voxel

```

接着我做了进一步的实验，我设置了一个滑动窗，目的是节省内存开销，仅保留最近3个batch的图像输入。这一步是为了后面的操作奠基，为了后面的操作实验了基本的可行性基础。

```text

python run_image_inference_batches.py --image-dir D:\3DV\STream3R\output\case0\images --output-dir D:\3DV\STream3R\output\case03 --frames-per-batch 3 --window-size 3 --save-glb --incremental-backend stream --fusion icp_voxel --save-per-frame-points --save-per-frame-glb

```

## 4.14 我这里尝试将经过DINO分割后的结果直接映射到点云中，以居家图16为例

```text

python d:\DINO\dinov3\vic_try\region_segmentation.py --image D:\DINO\dinov3\vic_try\images\000016.png --n-clusters 9 --long-side 896 --out-dir D:\DINO\dinov3\vic_try\outputs\home  # 先得到对应的分割图

python .\dealnpz\map_segmentation_to_pcd.py --npz "D:\3DV\STream3R\output\case0\predictions.npz" --frame-index 16 --pcd "D:\3DV\STream3R\output\case0\pcd_frames\frame_016.pcd" --seg-image "D:\3DV\STream3R\output\case0\000016_kmeans_overlay_k4.png" --reference-image "D:\3DV\STream3R\output\case0\images\000016.png" --align scale --label-mode kmeans --k 4 --output-dir "D:\3DV\STream3R\output\case0\semantic_backproj_016"    #从之前的分割图中进行相关处理

```

效果出奇的好，这里我认为至少有两个方面值得进一步改进：1. 思考点云补全方向的工作？主要是可以补全不齐整的点云，通过文本语义反馈图像语义，进而反馈点云2. 这里用DINO区分物体边界，但是实际由于受到分割数量的限制，这里和之前一样，可以用文本信息加以限制。

此外，有相关失败实验。我尝试在windows4060单卡上跑一个初步的text-png对齐的模型，但是受到多卡通信设定限制，无论是升级cuda还是进行改进均以失败告终。这里反思应该还是环境问题，如果有多卡设备可以再次进行相关尝试。这一点后续得持续更进。

## 4.15 已经处理好了coco cap的数据集，并对应验证效果

```text

python .\try_txt_png\verify_coco_pair.py

```

这里我主动设置了随机种子，并实现验证数据库中文本图像的对齐工作表。

## 4.16 进行了小规模的冒烟实验

在2张5090显卡上进行了测试，在小batch_size基础上已经验证了方法以及代码的可行性，接下来就是大规模集群的验证过程。这里环境很重要，一定得是pytorch 2.7以上才能支持，整体对显卡的需求量还是较大。惟愿效果能好一点！

---